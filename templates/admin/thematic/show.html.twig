{% extends 'base.html.twig' %}

{% block title %}Thematic
{% endblock %}

{% block body %}
	<h1>Thematic</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ thematic.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ thematic.name }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ thematic.description }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('thematic_list') }}">back to list</a>

	<a href="{{ path('thematic_edit', {'id': thematic.id}) }}">edit</a>

	{{ include('/admin/thematic/_delete_form.html.twig') }}
{% endblock %}
