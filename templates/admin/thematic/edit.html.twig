{% extends 'base.html.twig' %}

{% block title %}Edition Thématique
{% endblock %}

{% block body %}
	<div class="card-body">
		<div class="table-responsive">
			<table class="table table-bordered table-sm" id="dataTable" width="100%" cellspacing="0">

				<h2>Edition Thématique</h2>

				{{ include('/admin/thematic/_form.html.twig', {'button_label': 'Mise à Jour'}) }}
				{{ include('/admin/thematic/_delete_form.html.twig') }}
			</table>
			<a class="btn btn-outline-secondary btn-sm" href="{{ path('thematic_list') }}" role="button">Retour à la liste</a>
		</div>
	</div>
{% endblock %}
