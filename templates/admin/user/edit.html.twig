{% extends 'admin/layout.html.twig' %}

{% block title %}
{% endblock %}

{% block content %}
<section class="py-4">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12 col-md-6">
                <table class="table table-bordered table-sm" id="dataTable" width="100%" cellspacing="0">
				<h2>Editer un utilisateur</h2>
                <div class="mb-3">
                    {{ include('/admin/user/_form.html.twig', {'button_label': 'Mise à jour'}) }}
                </div>
                    {{ include('/admin/user/_delete_form.html.twig') }}
                </table>
                    <a class="btn btn-outline-secondary btn-sm" href="{{ path('user_list') }}" role="button">Retour</a>
            </div>
        </div>
    </div>
</section>

{% endblock %}
