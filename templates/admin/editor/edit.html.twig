{% extends 'admin/layout.html.twig' %}

{% block title %}Edit Editor{% endblock %}

{% block content %}
<section class="py-4">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12 col-md-6">
                <h1>Edit Editor</h1>

                {{ include('/admin/editor/_form.html.twig', {'button_label': 'Update'}) }}
            
                <a href="{{ path('editor_list') }}">back to list</a>
            
                {{ include('/admin/editor/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</section>
  
{% endblock %}
