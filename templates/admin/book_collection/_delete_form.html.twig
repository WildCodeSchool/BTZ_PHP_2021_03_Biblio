<form method="post" action="{{ path('book_collection_delete', {'id': book_collection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book_collection.id) }}">
    <button class="btn">Delete</button>
</form>
