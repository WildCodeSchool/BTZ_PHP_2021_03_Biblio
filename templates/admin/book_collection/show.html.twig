{% extends 'base.html.twig' %}

{% block title %}BookCollection{% endblock %}

{% block body %}
    <h1>BookCollection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book_collection.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ book_collection.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('book_collection_list') }}">back to list</a>

    <a href="{{ path('book_collection_edit', {'id': book_collection.id}) }}">edit</a>

    {{ include('/admin/book_collection/_delete_form.html.twig') }}
{% endblock %}
