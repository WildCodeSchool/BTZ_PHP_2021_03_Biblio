{% extends 'base.html.twig' %}

{% block title %}KeywordGeo{% endblock %}

{% block body %}
    <h1>KeywordGeo</h1>
    <div class="table-responsive">
        <table class="table table-bordered table-sm" id="dataTable" width="100%" cellspacing="0">
            <tbody>
                {# <tr>
                    <th>Id</th>
                    <td>{{ keyword_geo.id }}</td>
                </tr> #}
                <tr>
                    <th>Nom</th>
                    <td>{{ keyword_geo.name }}</td>
                </tr>
                {# <tr>
                    <th>Slug</th>
                    <td>{{ keyword_geo.slug }}</td>
                </tr> #}
            </tbody>
        </table>
    </div>
    <a class="btn btn-outline-primary btn-sm" href="{{ path('keyword_geo_edit', {'id': keyword_geo.id}) }}" role="button">Edition</a>
    {{ include('/admin/keyword_geo/_delete_form.html.twig') }}
    <a class="btn btn-outline-secondary btn-sm" href="{{ path('keyword_geo_list') }}">Retour à la liste</a>
{% endblock %}
