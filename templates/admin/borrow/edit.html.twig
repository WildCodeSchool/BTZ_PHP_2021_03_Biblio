{% extends 'base.html.twig' %}

{% block title %}Edition des Emprunts{% endblock %}

{% block body %}
    <h1>Edition des Emprunts</h1>

    {{ include('admin/borrow/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('emprunt_list') }}">Retour à la liste</a>

    {{ include('admin/borrow/_delete_form.html.twig') }}
{% endblock %}
