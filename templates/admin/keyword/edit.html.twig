{% extends 'admin/layout.html.twig' %}

{% block title %}Edit Keyword{% endblock %}

{% block content %}
<section class="py-4">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12 col-md-6">
                <h1>Edit Keyword</h1>

                {{ include('/admin/keyword/_form.html.twig', {'button_label': 'Update'}) }}
            
                <a href="{{ path('keyword_list') }}">back to list</a>
            
                {{ include('/admin/keyword/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</section>
 
{% endblock %}
