{% extends 'base.html.twig' %}

{% block title %}Edition des Auteurs
{% endblock %}

{% block body %}
	<div class="card-body">
		<div class="table-responsive">
			<table class="table table-bordered table-sm" id="dataTable" width="100%" cellspacing="0">


				<h2>Edition Auteur</h2>

				{{ include('/admin/author/_form.html.twig', {'button_label': 'Mise à Jour'}) }}

				{{ include('/admin/author/_delete_form.html.twig') }}
				<div></div>
				<a href="{{ path('auteur_list') }}">Retour à la liste</a>

    {{ include('/admin/author/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('auteur_list') }}">back to list</a>

    {{ include('/admin/author/_delete_form.html.twig') }}

			</table>
		</div>
	</div>

{% endblock %}
