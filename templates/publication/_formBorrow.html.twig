{{ form_start(form, {'action': path('publication_borrow', { id: publication.id }), 'method': 'POST'}) }}

    <div class="row justify-content-center">
        <div class="col-md-6">
            {{ form_row(form.reservation_date) }}
        </div>
        
        <div class="col-md-6">
            {{ form_row(form.comment) }}
        </div>
        
    </div>

    <button class="btn btn-outline-success">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}
